<GameContainer
  @leftPlayerScore={{leftPlayerScore}}
  @rightPlayerScore={{rightPlayerScore}}
  @game="starships"
  @model={{shuffledModel}}
  @selectedAttr="length"
  @refreshRoute={{route-action "refreshRoute"}}/>
